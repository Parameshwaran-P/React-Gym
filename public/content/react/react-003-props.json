{
  "id": "react-003-props",
  "title": "Props (Component Communication)",
  "description": "Learn how data flows from parent to child components using props",
  "duration": 8,
  "difficulty": "beginner",
  "tags": ["props", "components", "data-flow"],
  "prerequisites": [],
  "unlocks": ["react-001-usestate"],
  "xp": 40,
   "preview": {
    "videoUrl": "/public/input.mp4", 
    "platform": "direct",
    "thumbnail": "/thumbnails/custom-hooks.jpg",
    "duration": "10:15"
  },
  "steps": {
    "refresher": {
      "type": "markdown",
      "title": "🏥 Real-Life Story: Hospital Report Delivery",
      "content": "# 📦 Hospital Report Delivery System\n\nImagine a hospital management system.\n\nA **doctor** writes a patient report.\n\nThat report must be:\n- Displayed on the **nurse’s screen**\n- Shown inside the **patient summary card**\n\nBut here's the rule 👇\n\n❌ Nurses and cards **cannot change** the report.\n\nThey can only **read what the doctor sends**.\n\n---\n\n## ❌ The Problem\n\nWithout a proper delivery system:\n\n- Data gets duplicated\n- Child components create their own data\n- Inconsistencies appear everywhere\n\n---\n\n## 🧠 Why This Happens\n\nEach React component is **isolated by default**.\n\nThey don’t know about each other’s data unless it’s passed explicitly.\n\n---\n\n## ✅ React Solution: Props\n\n**Props** are how a parent component **sends data** to a child component.\n\nProps are:\n- Read-only\n- One-way (parent → child)\n\n---\n\n## 🔧 Basic Syntax\n\n```js\nfunction Child({ data }) {\n  return <p>{data}</p>;\n}\n\n<Child data=\"Patient Report\" />\n```\n\n👉 **Next: Let’s deliver reports correctly.**"
    },
    "positive": {
      "type": "interactive-code",
      "title": "✅ Positive Case: Passing Patient Data",
      "description": "The doctor sends patient data to a summary card using props.",
      "code": "function PatientCard({ name, age }) {\n  return (\n    <div className=\"p-4 border rounded\">\n      <h3 className=\"font-bold\">Patient Info</h3>\n      <p>Name: {name}</p>\n      <p>Age: {age}</p>\n    </div>\n  );\n}\n\nfunction HospitalApp() {\n  return (\n    <div className=\"p-6\">\n      <PatientCard name=\"Arjun\" age={32} />\n    </div>\n  );\n}\n\nexport default HospitalApp;",
      "showPreview": true,
      "explanation": "**How it works (Hospital Mapping):**\n\n1. Doctor (parent) owns the data\n2. Report is passed to the nurse card via props\n3. Child displays the data\n4. No direct modification allowed\n\n✅ **Key Rule:** Props flow downward only."
    },
    "negative": {
      "type": "debug-quiz",
      "title": "🐛 Common Mistake: Modifying Props",
      "description": "This nurse tries to change the doctor’s report. What’s wrong?",
      "code": "function PatientCard({ age }) {\n  age = age + 1;\n  return <p>Age: {age}</p>;\n}",
      "question": "Why is this code wrong?",
      "options": [
        {
          "id": "a",
          "text": "Props cannot be reassigned",
          "isCorrect": true,
          "explanation": "✅ Correct! Props are read-only. Only the parent can change data."
        },
        {
          "id": "b",
          "text": "JavaScript does not allow reassignment",
          "isCorrect": false,
          "explanation": "JavaScript allows reassignment, but React props must remain immutable."
        },
        {
          "id": "c",
          "text": "Props must always be strings",
          "isCorrect": false,
          "explanation": "Props can be any type."
        },
        {
          "id": "d",
          "text": "The component syntax is invalid",
          "isCorrect": false,
          "explanation": "The syntax is valid, but the logic is incorrect."
        }
      ],
      "correctCode": "function PatientCard({ age }) {\n  return <p>Age: {age}</p>;\n}",
      "lesson": "**Real-Life Mistake:**\n\nThis is like a nurse editing the doctor’s report.\n\n❌ Causes data inconsistency\n\n✅ Only the source (parent) can update data."
    },
    "task": {
      "type": "coding-task",
      "title": "🛠️ Your Turn: Medicine Label",
      "description": "Create a MedicineLabel component that receives name and dosage via props.",
      "requirements": [
        "Accept name and dosage as props",
        "Display both values",
        "Do not modify props"
      ],
      "starterCode": "function MedicineLabel() {\n  return (\n    <div className=\"p-4\">\n      {/* TODO: Show medicine info */}\n    </div>\n  );\n}\n\nexport default MedicineLabel;",
      "solution": "function MedicineLabel({ name, dosage }) {\n  return (\n    <div className=\"p-4 border rounded\">\n      <p>Medicine: {name}</p>\n      <p>Dosage: {dosage}</p>\n    </div>\n  );\n}\n\nexport default MedicineLabel;",
      "hints": [
        "Props come from parent",
        "Use destructuring",
        "Props are read-only"
      ],
      "tests": [
        "Displays medicine name",
        "Displays dosage",
        "No mutation of props"
      ]
    },
    "challenge": {
      "type": "coding-challenge",
      "title": "🏆 Challenge: Patient List",
      "description": "Build a list where the parent passes patient data to multiple cards.",
      "requirements": [
        "Parent holds patient array",
        "Child receives data via props",
        "Render multiple PatientCard components"
      ],
      "starterCode": "function PatientCard({ name }) {\n  return <p>{name}</p>;\n}\n\nfunction PatientList() {\n  // TODO: Create patient data and pass it\n}\n\nexport default PatientList;",
      "solution": "function PatientCard({ name }) {\n  return <p>{name}</p>;\n}\n\nfunction PatientList() {\n  const patients = ['Arjun', 'Meena', 'Ravi'];\n\n  return (\n    <div>\n      {patients.map((name) => (\n        <PatientCard key={name} name={name} />\n      ))}\n    </div>\n  );\n}\n\nexport default PatientList;",
      "hints": [
        "Data lives in parent",
        "Use map()",
        "Pass props correctly"
      ],
      "tests": [
        "All patients render",
        "Props passed correctly",
        "No state duplication"
      ]
    }
  }
}
